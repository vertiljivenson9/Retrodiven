export interface WindowState { id: string; appId: string; title: string; isMaximized: boolean; isMinimized: boolean; zIndex: number; icon: string; initialData?: any; }
export interface OSState { booted: boolean; isLocked: boolean; authorizedUser: string; wallpaper: string; windows: WindowState[]; files: any[]; installedApps: any[]; processes: any[]; githubRepo?: string; githubToken?: string; }