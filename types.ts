export interface WindowState { id: string; appId: string; title: string; isMaximized: boolean; isMinimized: boolean; zIndex: number; icon: string; initialData?: any; orientation?: 'portrait' | 'landscape'; }
export interface OSState { booted: boolean; isLocked: boolean; isOff: boolean; authorizedUser: string; wallpaper: string; windows: WindowState[]; activeWindowId: string | null; githubRepo?: string; githubToken?: string; files: any[]; installedApps: any[]; processes: any[]; services: any[]; audioSettings: any; allowUnknownSources: boolean; manualBypassAllowed: boolean; systemDirty?: boolean; }